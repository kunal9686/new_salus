"use client";

/**
 * GlobalAudioPlayer has been removed as per user request.
 * This file is kept as a placeholder to prevent import breakages if any references remain.
 */
export function GlobalAudioPlayer() {
  return null;
}
